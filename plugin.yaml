name: libravdb
version: 0.5.3
description: "Native LibraVDB memory provider for Hermes Agent — gRPC-backed semantic memory with session, user, and global scopes."
pip_dependencies:
  - "grpcio>=1.60"
  - "libravdb-contracts>=2.0.5"
hooks:
  - on_session_start
  - on_session_end
  - on_session_finalize
  - on_session_reset